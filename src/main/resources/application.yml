spring:
  application:
    name: auto-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/auto-service
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create